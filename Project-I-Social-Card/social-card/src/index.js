// dependency imports
import React from 'react';
import ReactDOM from 'react-dom';

// styling imports
import './index.css';

// component imports
import App from './App';

// render App component that was imported and mount to root
ReactDOM.render(<App />, document.getElementById('root'));
